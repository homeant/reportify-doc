---
title: '交易日历'
description: '获取指定日期区间内的 A 股交易日列表'
openapi: 'GET /v1/quant/quotes/calendar'
---

获取指定日期区间内的交易日列表。日历数据来自交易所，采用纯 Python 解码，覆盖范围从 1990 年至已公布的未来交易日。

<Note>
目前仅支持 **A 股**（`market=cn`）。港股、美股等暂未支持，传入会返回错误。
</Note>

<CodeGroup>

```bash cURL
curl "https://api.reportify.cn/v1/quant/quotes/calendar?start_date=2026-01-01&end_date=2026-01-31" \
  -H "Authorization: Bearer <token>"
```

```python Python
import requests

response = requests.get(
    "https://api.reportify.cn/v1/quant/quotes/calendar",
    headers={"Authorization": "Bearer <token>"},
    params={
        "start_date": "2026-01-01",
        "end_date": "2026-01-31",
    }
)
print(response.json())
```

```typescript TypeScript
const params = new URLSearchParams({
  start_date: '2026-01-01',
  end_date: '2026-01-31'
});

const response = await fetch(
  `https://api.reportify.cn/v1/quant/quotes/calendar?${params}`,
  {
    headers: { 'Authorization': 'Bearer <token>' }
  }
);
const data = await response.json();
```

</CodeGroup>

## 请求参数

<ParamField query="start_date" type="string">
  开始日期（`YYYY-MM-DD`），可选，默认当年 1 月 1 日
</ParamField>

<ParamField query="end_date" type="string">
  结束日期（`YYYY-MM-DD`），可选，默认当年 12 月 31 日
</ParamField>

<ParamField query="market" type="string" default="cn">
  股票市场。目前仅支持 `cn`（A 股）
</ParamField>

<ParamField query="exchange" type="string">
  交易所，可选值 `sh`（上交所）、`sz`（深交所）。不传时使用市场默认交易所（A 股为 `sh`）
</ParamField>

## 响应参数

<ResponseField name="datas" type="array">
  交易日列表（按日期升序）
  <Expandable title="数据字段">
    <ResponseField name="date" type="string">
      交易日（`YYYY-MM-DD`）
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="metadata" type="object">
  查询元数据，包含 `market`、`exchange`、`start_date`、`end_date`、`count`
</ResponseField>

## 响应示例

```json
{
  "datas": [
    { "date": "2026-01-05" },
    { "date": "2026-01-06" },
    { "date": "2026-01-07" }
  ],
  "metadata": {
    "market": "cn",
    "exchange": "sh",
    "start_date": "2026-01-01",
    "end_date": "2026-01-31",
    "count": 20
  }
}
```
